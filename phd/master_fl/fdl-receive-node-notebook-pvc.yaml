apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: node-notebook-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi  # Adjust storage size based on your requirements
  storageClassName: standard

